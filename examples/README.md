# Examples

Runnable, self-contained examples.

- `basic/` — generate a badge for a repo from the command line.

For automating the badge in CI, see
[`../.github-workflow-example/carbon-badge.yml`](../.github-workflow-example/carbon-badge.yml).
